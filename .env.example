# Local tooling configuration only.
# Copy this file to .env and set every value locally.
# Do not commit machine-specific values here.
#
# Never store TOTP secrets, otpauth payloads, Wi-Fi passwords, tokens,
# private keys, or other authentication material in .env.

M5AUTH_IDF_VERSION=
M5AUTH_CHIP=
M5AUTH_PORT=
